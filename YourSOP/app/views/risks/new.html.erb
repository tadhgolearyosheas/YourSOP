<div class="page-header">
  <h1>Risk Assessment Form</h1>
  <h2>Topic: <%= @topic.name %></h2>
</div>
<div class="container">
<%= render partial: 'form', locals: {risk: @risk, current_user_id: @current_user_id, risk_impact: @risk_impact, risk_likelihood: @risk_likelihood, current_user_name: @current_user_name, documents: @documents } %>
</div>
