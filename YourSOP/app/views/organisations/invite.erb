
<%= bootstrap_form_for :organisation_user,  layout: :horizontal, label_col: "col-sm-1", control_col: "col-sm-6" do |form| %>

  <%= form.text_field :email %>


  <%# <%= form.autocomplete_field :user_email, autocomplete_user_email_organisations_path,
  <%#   :size => 30 %>

  <%# <%= form.select(:invitedID, @users, {:prompt => "select user to invite"}, { :multiple => true, :size => 20 } ) %>

  <%= form.select(:typesSelection, @typesOptions, label: "Select  Role :") %>


	<%= form.form_group do %>
  		<%= form.submit "Invite", class: "btn btn-primary" %>
  	<% end %>



<% end %>
